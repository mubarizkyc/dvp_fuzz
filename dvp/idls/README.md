# IDL Files

Place your program's IDL JSON file here as `dvp.json`.

## Generating IDL

If you have the legacy (v0.29) IDL format:
```bash
anchor idl convert target/idl/dvp.json -o fuzz/dvp/idls/dvp.json
```

If you have the new IDL format (v0.30+), copy it directly.
