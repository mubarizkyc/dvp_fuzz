Pay special attention to the dependencies. The fuzzer uses the latest version of Crucible and other dependencies, including specific versions and features.
to run 
```
crucible run dvp invariant_test  --corpus-in /every_time_new_folder_so_the_fuzzer_dosent_uses_corpus
```
